import { createLabel } from './public/helpers/createLabel';

describe('1 - Teste a função createLabel', () => {
  it('1. Teste se createLabel é uma função', () => {
    expect(typeof createLabel).toBe('function');
  });
});
