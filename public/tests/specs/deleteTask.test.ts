import deleteTask from '../mocks/deleteTask';

describe('1 - Teste a função addLabel', () => {
  it('1 - Testar se addLabel é um função', () => {
    expect(typeof deleteTask).toBe('function');
  });
});